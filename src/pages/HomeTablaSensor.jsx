import SensoresList from "../components/SensoresList";

const HomeTablaSensor = () => {
    return (
        <div>
            <SensoresList />
        </div>
    );
};

export default HomeTablaSensor;